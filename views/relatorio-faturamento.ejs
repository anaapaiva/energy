<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Relatório de Faturamento</title>
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f5f5f5;
        color: #222;
        margin: 0;
        padding: 0;
      }

      .container {
        max-width: 900px;
        margin: 40px auto;
        background-color: #fff;
        padding: 20px;
        border-radius: 12px;
        box-shadow: 0 -3px 10px rgba(0, 0, 0, 0.15);
        border: 1px solid #ccc;
      }

      header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 20px;
      }

      .back-btn {
        background-color: #222;
        color: #fff;
        padding: 10px 18px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: bold;
        transition: 0.3s;
      }

      .back-btn:hover {
        background-color: #000;
      }

      h1 {
        font-size: 1.8em;
        color: #333;
        text-align: center;
        flex: 1;
      }

      .report-box {
        text-align: center;
        font-size: 1.2em;
        padding: 20px;
        border-radius: 8px;
        background-color: #333;
        color: #fff;
        border: 1px solid #000;
      }

      @media (max-width: 600px) {
        .container {
          margin: 20px;
          padding: 15px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <a href="/vendedor" class="back-btn">← Voltar</a>
        <h1>Relatório de Faturamento</h1>
      </header>

      <div class="report-box" id="faturamento">
        Faturamento Total: R$ <%= Number(totalFaturado).toFixed(2) %>
      </div>
    </div>

    </body>
</html>